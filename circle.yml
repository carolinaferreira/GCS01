machine:
  post:
    - pyenv global 2.6 2.7
dependecies:
  pre:
   - pip install PIL
   - pip install django flake8
compile:
  - python manage.py migrate
test:
  override:
    - python manage.py test
    - flake8 --exclude polls/migrations/,manage.py  .
